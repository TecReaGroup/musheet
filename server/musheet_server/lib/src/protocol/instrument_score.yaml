class: InstrumentScore
table: instrument_scores
fields:
  scoreId: int, relation(parent=scores)
  instrumentName: String
  pdfPath: String?
  pdfHash: String?
  orderIndex: int
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime?
  version: int
  syncStatus: String?
indexes:
  instrument_score_idx:
    fields: scoreId
    type: btree
  instrument_score_unique:
    fields: scoreId, instrumentName
    unique: true
  instrument_score_version_idx:
    fields: scoreId, version
    type: btree