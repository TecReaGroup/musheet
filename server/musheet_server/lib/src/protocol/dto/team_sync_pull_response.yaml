### Team Sync Pull Response DTO
### Response containing team changes since a given version
class: TeamSyncPullResponse
fields:
  ### Current team library version on server
  teamLibraryVersion: int

  ### Whether this is a full sync (since=0)
  isFullSync: bool

  ### Team scores modified since the given version
  teamScores: List<SyncEntityData>?

  ### Team instrument scores modified since the given version
  ### Includes pdfHash and annotationsJson
  teamInstrumentScores: List<SyncEntityData>?

  ### Team setlists modified since the given version
  teamSetlists: List<SyncEntityData>?

  ### Team setlist scores modified since the given version
  teamSetlistScores: List<SyncEntityData>?

  ### List of deleted entity IDs (format: "type:id")
  deleted: List<String>?
