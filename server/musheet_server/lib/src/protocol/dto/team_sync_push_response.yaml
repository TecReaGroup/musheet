### Team Sync Push Response DTO
### Response from team push operation
class: TeamSyncPushResponse
fields:
  ### Whether the push was successful
  success: bool

  ### New team library version after push (if successful)
  newTeamLibraryVersion: int?

  ### List of accepted entity IDs
  accepted: List<String>?

  ### Map of local entity ID to server ID for newly created entities
  serverIdMapping: Map<String, int>?

  ### Whether a conflict occurred (HTTP 412 equivalent)
  conflict: bool

  ### Current server team library version (if conflict)
  serverTeamLibraryVersion: int?

  ### Error message if any
  errorMessage: String?
