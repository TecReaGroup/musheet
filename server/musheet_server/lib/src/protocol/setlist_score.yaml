class: SetlistScore
table: setlist_scores
fields:
  setlistId: int, relation(parent=setlists)
  scoreId: int, relation(parent=scores)
  orderIndex: int
indexes:
  setlist_score_idx:
    fields: setlistId
    type: btree